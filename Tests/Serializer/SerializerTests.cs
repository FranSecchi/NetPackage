using UnityEngine;

namespace SerializerTest.NetPackage.Tests.Serializer
{
    public class SerializerTests : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
